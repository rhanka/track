node_modules/
dist/
*.log
.DS_Store

# Handover notes are session-local and must NOT be committed
HANDOVER.md

# Harness session artifacts
.claude/scheduled_tasks.lock

# Playwright MCP session artifacts
.playwright-mcp/

# Demand lifecycle (Mode A) — the EPHEMERAL lease side-store is mutable, host-local, and NOT the
# append-only event log (heartbeats must never be committed). Advisory + reconstructible.
.track/leases.json
